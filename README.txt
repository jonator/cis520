LAB1 README

Changes required to make a new test:

Add a new entry to the array of tests in tests.c. A string key of the test name, and the name of an external funciton, test_alarm_mega. Need to include the external function in the header file.

Add name of test in Rubric.alarm.

Add implementation of test in alarm-wait.c; test_alarm_mega.

Add perl script with an implementation of the test, running at 70 instead of 7.

